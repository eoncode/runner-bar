name: Swift Test

on:
  pull_request:

jobs:
  test:
    name: Swift Test
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run tests
        run: swift test --filter RunnerBarCoreTests
