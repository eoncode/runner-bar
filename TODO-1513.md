# TODO — Issue #1513: Move free functions onto `struct GitHubTransport: GitHubTransportProtocol`

Ref: https://github.com/eoncode/runner-bar/issues/1513  
Prerequisite for: Item 4 and Item 8 (Track A)

---

## Step 1 — Define `GitHubTransportProtocol` in `GitHubURLSessionTransport.swift`

- [ ] Add `public protocol GitHubTransportProtocol: Sendable` with all method signatures that the free functions currently expose:
  - `apiAsync(_:timeout:)`
  - `apiPaginated(_:timeout:)`
  - `raw(_:timeout:)`
  - `post(_:body:timeout:)`
  - `put(_:body:timeout:)`
  - `delete(_:timeout:)`
  - `cancelRun(runID:scope:)`
  - `patchRunnerLabels(scope:runnerID:labels:)`
  - `fetchRegistrationToken(scope:)`
  - `fetchRemovalToken(scope:)`
  - `deleteRunnerByID(scope:runnerID:)`
- [x] Commit: `feat(#1513): define GitHubTransportProtocol`

---

## Step 2 — Create `struct GitHubTransport` shell with stored properties

- [ ] Add `public struct GitHubTransport: GitHubTransportProtocol` below the protocol
- [ ] Move `sharedDecoder` and `sharedEncoder` from module globals into stored `let` properties on the struct
- [ ] Add `init(decoder:encoder:rateLimiter:tokenProvider:)` with defaults that preserve existing runtime behaviour
- [ ] Commit: `feat(#1513): add GitHubTransport struct shell with stored properties`

---

## Step 3 — Move `urlSessionExecute` onto the struct as `private func execute`

- [ ] Copy `private func urlSessionExecute(...)` body onto `struct GitHubTransport` as `private func execute(...)`
- [ ] Replace all references to module-level `rateLimitActor` → `self.rateLimiter`, `githubTokenCore()` → `tokenProvider()`, `sharedDecoder` → `self.decoder`, `sharedEncoder` → `self.encoder`
- [ ] Remove (or keep as thin shim) the old module-level `urlSessionExecute` — keep shim until Step 4 removes callers
- [ ] Commit: `feat(#1513): move urlSessionExecute onto struct as private execute`

---

## Step 4 — Move all public transport functions onto the struct

- [ ] Move each public free function body onto the struct as a conforming method:
  - `urlSessionAPIAsync` → `apiAsync`
  - `urlSessionAPIPaginated` → `apiPaginated`
  - `urlSessionRaw` → `raw`
  - `urlSessionPost` → `post`
  - `urlSessionPut` → `put`
  - `urlSessionDelete` → `delete`
  - `ghAPI` → (keep shim; struct method is the impl)
  - `cancelRun` → `cancelRun`
  - `patchRunnerLabels` → `patchRunnerLabels`
  - `fetchRegistrationToken` → `fetchRegistrationToken`
  - `fetchRemovalToken` → `fetchRemovalToken`
  - `deleteRunnerByID` → `deleteRunnerByID`
- [ ] Add `internal let sharedGitHubTransport = GitHubTransport()` as the default singleton
- [ ] Replace each old free function body with a one-liner forwarding call to `sharedGitHubTransport`
- [ ] Commit: `feat(#1513): move public transport funcs onto struct, add sharedGitHubTransport shims`

---

## Step 5 — Update `GitHubTransportShim.swift` wiring to use the struct

- [ ] Update the `configure*` call sites at app launch to wire through `sharedGitHubTransport`:
  - `configureGHAPI { await sharedGitHubTransport.apiAsync($0) }`
  - `configureGHRaw { await sharedGitHubTransport.raw($0) }`
  - `configureGHAPIPaginated { await sharedGitHubTransport.apiPaginated($0, timeout: $1) }`
- [ ] Verify `configureGHToken` still routes through `tokenProvider` on the struct init
- [ ] Commit: `feat(#1513): wire GitHubTransportShim configure calls through struct`

---

## Step 6 — Build + test verification

- [ ] `swift build` — zero errors, zero warnings
- [ ] `swift test` — all existing transport tests green (`GitHubTransportShimTests`, `GitHubTransportPaginatedTests`, `GitHubRateLimitActorTests`)
- [ ] Commit any fixups under: `fix(#1513): post-refactor build/test fixups`

---

## Step 7 — Cleanup & PR

- [ ] Remove `TODO-1513.md` from repo (or move to docs)
- [ ] Push branch and open PR referencing #1513
- [ ] Commit: `chore(#1513): open PR, cleanup TODO`
